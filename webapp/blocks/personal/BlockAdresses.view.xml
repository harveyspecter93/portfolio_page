<mvc:View xmlns:mvc="sap.ui.core.mvc" xmlns:forms="sap.ui.layout.form"
		  xmlns:core="sap.ui.core" xmlns="sap.m">

	<forms:SimpleForm layout="ColumnLayout" editable="false" width="100%">

		<core:Title text="{i18n>personal.connect.address.title}"/>

		<Label text="{i18n>personal.connect.address.home.label}"/>
		<Text text="{i18n>personal.connect.address.home.text}"/>
		<Label text="{i18n>personal.connect.address.office.label}"/>
		<Text text="{i18n>personal.connect.address.office.text}"/>
		

	</forms:SimpleForm>
</mvc:View>
