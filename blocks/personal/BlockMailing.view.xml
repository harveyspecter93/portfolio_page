<mvc:View
		xmlns:mvc="sap.ui.core.mvc"
		xmlns:forms="sap.ui.layout.form"
		xmlns:core="sap.ui.core"
		xmlns="sap.m">

	<forms:SimpleForm layout="ColumnLayout" width="100%">

			<core:Title text="{i18n>personal.connect.mail.title}"/>

		<Label text="{i18n>personal.connect.mail.private.label}"/>
		<Text text="{i18n>personal.connect.mail.private.text}"/>
		<Label text="{i18n>personal.connect.mail.office.label}"/>
		<Text text="{i18n>personal.connect.mail.office.text}"/>

	</forms:SimpleForm>
</mvc:View>
