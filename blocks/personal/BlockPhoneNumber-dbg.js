sap.ui.define(['sap/uxap/BlockBase'],
	function (BlockBase) {
		"use strict";

		var BlockPhoneNumber = BlockBase.extend("ch.draeyer.mike.portfolio.blocks.personal.BlockPhoneNumber", {
			metadata: {}
		});


		return BlockPhoneNumber;

	});
